<%= form_for @student_profile, html: { multipart: true } do |f| %>
  <% if student_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student_profile.errors.count, "error") %> prohibited this student_profile from being saved:</h2>

      <ul>
      <% student_profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :studentID %>
    <%= f.text_field :studentID %>
  </div>

  <div class="field">
    <%= f.label :phoneNo %>
    <%= f.text_field :phoneNo %>
  </div>

  <div class="field">
    <%= f.label :fos %>
    <%= f.text_field :fos %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image %>
  </div >

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
